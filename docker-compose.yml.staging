version: '3'
services:
  app:
    command:  sh -c "bundle exec rails s -b 0.0.0.0 -p 3000 -P /tmp/server.pid"
    restart: always
    ports:
      # site
      - "3100:3000"
    environment:
      RAILS_ENV: production
      RAILS_SERVE_STATIC_FILES: 1
      DISABLE_DATABASE_ENVIRONMENT_CHECK: 1

